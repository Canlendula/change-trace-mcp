{
  "schemaVersion": "1.0.0",
  "fixtureId": "missing-permissions",
  "outcome": "inconclusive",
  "minFindings": 1,
  "maxFindings": 5,
  "requiredMatches": [
    {
      "category": "other",
      "status": "inconclusive",
      "recommendation": "investigate",
      "requiredEvidenceIds": ["evidence:doc:missperm-spec", "evidence:diff:missperm-auth"],
      "minCount": 1
    }
  ],
  "forbiddenStatuses": ["confirmed", "suspected"],
  "rationale": "The .env.production evidence is inaccessible; findings must remain inconclusive and not claim hidden content."
}
