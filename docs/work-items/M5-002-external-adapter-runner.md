# M5-002 — Execute and normalize a preconfigured external adapter

## Assignment — coordinator owned

- Status: `assigned`
- Milestone: `M5 — External documentation adapters`
- Base commit: `3b94c78fde7ef14a0c021042f4d9d0464b6aac3d`
- Branch: `codex/M5-002-external-adapter-runner`
- Worktree:
  `D:\projects\change-trace-worktrees\M5-002-external-adapter-runner`
- Push task branch: `no`
- Objective: implement the bounded Host-owned command boundary that sends an
  accepted M5-001 request to a preconfigured adapter, validates one strict
  response, and normalizes it into provenance-rich external evidence without
  adding an MCP tool or user-facing configuration loader.
- Dependencies: accepted M5-001 and Decisions 10, 23, and 24.

### Coordinator-owned execution contract

The runner receives two programmatic inputs from trusted Host code:

1. a validated registration fixed outside Agent-controlled MCP input;
2. an M5-001 `ExternalAdapterRequest`.

The registration contains:

- the expected adapter ID, name, and version;
- one explicit argv array whose first entry is the executable;
- an allowlist of source-system names;
- an allowlist of credential environment-variable names;
- timeout, stdout-byte, and stderr-byte limits.

It contains no credential values. The runner builds a fresh child environment
from a small documented operating-system baseline plus only the configured
credential names found in the Host environment. It must not spread the Host
environment into the child.

The runner executes the argv directly with `shell: false`, writes exactly one
validated JSON request to stdin, accepts exactly one JSON response on stdout,
drains but never returns/logs/persists stderr, and terminates the child on
timeout or either output limit. Its public errors expose only bounded stable
codes and safe numeric exit metadata; they do not include argv, environment
names/values, stdout, stderr, response content, or document excerpts.

Before normalization it enforces:

- request `adapterId`, registration identity, and response identity match;
- every request ID has exactly one result and no extra result exists;
- result source type matches its request;
- request and result source systems are allowlisted and a result cannot change
  source system while canonicalizing locator/URI;
- all M5-001 runtime refinements pass.

Protocol or infrastructure failure rejects the entire invocation. Structured
`not_found`, `permission_denied`, `unsupported`, and adapter `error` results
remain valid evidence-availability outcomes.

### Coordinator-owned normalization contract

Available results become deterministic `EvidenceItem` records:

- core ID generated by Change Trace from stable adapter/request/source
  identity;
- type `document`;
- canonical result source and retrieval time;
- SHA-256 of the complete pre-redaction excerpt, or `null` when the adapter
  reports truncation;
- related change IDs and selection reason copied from the matching request;
- secret-like content redacted before the excerpt leaves the runner;
- trust forced to `untrusted_external`;
- truncation metadata retained with the final excerpt length;
- strict `externalProvenance` containing the configured adapter identity,
  source type, title, and nullable source update time.

Unavailable results become `MissingEvidence` in request order:

- `not_found` -> `not_found`;
- `permission_denied` -> `inaccessible`;
- `unsupported` -> `unsupported`;
- adapter `error` -> `inaccessible`.

Diagnostic messages are secret-redacted before becoming bounded reasons.

The returned `ExternalEvidenceCollection` contains the configured adapter
identity, evidence items, and missing evidence. It enforces
`untrusted_external`, external provenance, adapter match, and a combined
maximum of 100 outcomes.

### In scope

- Add a strict external adapter registration schema/type with hard bounds,
  uniqueness checks, safe environment-name validation, and explicit argv.
- Add a typed bounded runner error vocabulary and runner implementation.
- Add the external provenance and collection schemas/types from Decision 24.
- Refactor the M5-001 source-type/adapter-identity schema definitions only as
  needed to avoid module cycles; preserve their public exports and behavior.
- Add deterministic JSON Schema export for `externalEvidenceCollection`.
- Add deterministic normalization, hashing, redaction, trust assignment,
  provenance, missing-status mapping, and request-order preservation.
- Add local fixture adapter processes and focused tests for:
  - success with reordered response results;
  - structured missing/denied/unsupported/error outcomes;
  - injection-shaped excerpts treated as data;
  - secret redaction in excerpts and missing reasons;
  - minimal child environment and allowlisted credential forwarding;
  - registration/request/response identity mismatch;
  - missing, duplicate, extra, wrong-type, wrong-system, malformed, and
    schema-invalid results;
  - nonzero exit, spawn failure, timeout, stdout limit, and stderr limit;
  - stdout/stderr/credential/argv content absent from public errors;
  - direct-child termination and clean test teardown;
  - deterministic IDs/order/JSON Schema and existing-schema compatibility.

### Out of scope

- Reading a registration file, CLI environment option, repository
  configuration, or MCP tool input.
- Adding `collect_external_evidence`, server registration, review-bundle
  merging, reporting, caching, search, discovery, or source-key inference.
- Network calls, live credentials, live Lark/Jira/Confluence access, browser
  access, or vendor SDKs.
- Logging or returning raw adapter stdout/stderr.
- Passing the complete Host environment to a child.
- Retrying, repairing, or semantically interpreting an adapter response.
- Changing dependencies, lockfile, package version, Roadmap, decisions,
  governance, CI, release, or npm state.

### Allowed paths

- `src/schemas/external-adapter.ts`
- `src/schemas/external-provenance.ts`
- `src/schemas/external-evidence.ts`
- `src/schemas/evidence.ts`
- `src/schemas/index.ts`
- `src/schemas/json-schema.ts`
- `src/evidence/external/index.ts`
- `src/evidence/external/run-external-adapter.ts`
- `src/evidence/index.ts`
- `tests/fixtures/external-adapter/**`
- `tests/unit/external-evidence-schema.test.ts`
- `tests/integration/external-adapter-runner.test.ts`
- `tests/unit/core-schemas.test.ts` — only proportional compatibility/export
  assertions
- `docs/work-items/M5-002-external-adapter-runner.md` — Worker handoff only

### Coordinator-only paths

- `docs/ROADMAP.md`
- `docs/PROJECT_DECISIONS.md`
- `docs/evaluation/**`
- `AGENTS.md`
- `docs/CONTRIBUTING_WORKFLOW.md`
- `docs/work-items/README.md`
- `docs/work-items/TEMPLATE.md`
- this task's Assignment and Coordinator review sections
- `.github/**`
- `src/server.ts`
- `src/cli.ts`
- `src/evidence/bundle/**`
- `src/security/**`
- `package.json`
- `package-lock.json`
- package version, dependency, release, tag, and publishing metadata

### Acceptance criteria

- [ ] Registration is strict and bounded; expected adapter identity, argv,
      source allowlist, credential environment-name allowlist, timeout, and
      byte limits are fixed outside the request.
- [ ] The child runs with `shell: false`, receives one request on stdin, and
      receives only the documented baseline environment plus configured
      credential names.
- [ ] Timeout, stdout, and stderr limits terminate the child and return stable
      bounded errors without raw process or credential content.
- [ ] Only one strict response is accepted; request/registration/response
      identity, result coverage, source type, and source-system checks pass
      before normalization.
- [ ] Available content is redacted, hashed when complete, assigned a
      deterministic Change Trace ID, forced to `untrusted_external`, and keeps
      structured adapter/source provenance.
- [ ] Structured unavailable outcomes map deterministically to
      `MissingEvidence`, with secret-redacted reasons.
- [ ] Collection ordering follows request ordering even when response results
      are reordered.
- [ ] External evidence and unavailable outcomes have a combined hard maximum
      of 100.
- [ ] Prompt-injection-shaped content remains inert excerpt data.
- [ ] Existing schema IDs, M5-001 exports, MCP tool list, and package
      dependencies/version remain unchanged.
- [ ] Focused tests, type checking, two consecutive full-suite runs, stdio
      smoke, package dry-run, diff checks, and clean worktree checks pass.
- [ ] No registration loader, MCP change, bundle merge, network/live vendor
      access, retry/repair, dependency, Roadmap, decision, CI, release, or npm
      state change occurs.

### Required validation

```text
npx vitest run tests/unit/external-adapter-schema.test.ts tests/unit/external-evidence-schema.test.ts tests/integration/external-adapter-runner.test.ts tests/unit/core-schemas.test.ts
npm run check
npm test
npm test
npm run smoke:stdio
npm run pack:check
git diff --check 3b94c78fde7ef14a0c021042f4d9d0464b6aac3d..HEAD
git status --short
```

### Mandatory implementation sequence

1. Read `AGENTS.md`, `docs/CONTRIBUTING_WORKFLOW.md`, this complete task,
   Decisions 10, 23, and 24, the M5 Roadmap section, accepted M5-001, existing
   evidence/review-bundle schemas, secret redaction, Git subprocess handling,
   and focused tests.
2. Confirm the isolated worktree, branch, and exact base relationship.
3. Write failing schema and runner tests, including the environment and
   secret-safe error assertions, before production implementation.
4. Implement the smallest schema, runner, and normalizer changes satisfying
   the contract.
5. Audit the complete diff for shell use, Host-environment spread, credential
   values, unbounded buffers/timers, child leaks, stdout/stderr exposure,
   response ambiguity, coverage gaps, trust elevation, lost provenance,
   unstable IDs/order, and out-of-scope server/bundle work.
6. Run every required validation and review the complete base diff.
7. Update only the Worker handoff section, commit all output, leave the
   worktree clean, and report `ready_for_review`.

### Escalate when

- raw stdout/stderr or a credential value must enter an error, log, artifact,
  response, or fixture assertion;
- the request must supply a command, environment field, credential, search
  query, or trust value;
- an existing core schema must become incompatible or its version/ID must
  change;
- server, CLI, bundle, report, dependency, or another path outside the
  allowlist is required;
- reliable process termination cannot be tested on the current platform;
- live vendor/network access or a product decision beyond Decisions 23–24 is
  required.

## Worker handoff — worker owned

- Status: `not_started`
- Handoff branch:
- Implementation commits:

### Implementation summary

- Pending.

### Changed areas

- Pending.

### Validation

- Pending.

### Public contract and documentation impact

- Pending.

### Deviations from assignment

- None recorded.

### Known limitations and risks

- Pending.

### Decisions or questions for coordinator

- None.

### Protected-file confirmation

- [ ] Coordinator-only files were not modified by the worker.
- [ ] No version, dependency, tag, publish, live external-system, CI, or
      release action was performed.
- [ ] All intended handoff changes are committed to the task branch.

## Coordinator review — coordinator owned

- Outcome: `changes_requested`
- Reviewed branch head:
  `99d16f656776f59c6787ed84f70924b2170fd51a`
- Integration commit:

### Review findings

- The runner decodes stdout with `Buffer.toString("utf8")`. Node replaces
  malformed byte sequences with U+FFFD, so invalid UTF-8 embedded inside an
  otherwise valid JSON string can pass `JSON.parse` and the response schema.
  Adapter protocol JSON must be valid UTF-8; decode with a fatal UTF-8 decoder
  and return the existing safe `invalid_response` code without exposing
  content. Add a binary-output fixture and regression assertion.
- The Worker handoff records implementation commit
  `e38ccd06a790add8515d87355182f2e97ef5cd27`, but the actual repository object
  is `e38ccd0c1a16763869984ed95662dc91c2319558`. Correct the durable handoff and
  record the new follow-up commit/head exactly.
- The disclosed Windows bootstrap environment was independently reproduced:
  Node injects the documented system/user/session bootstrap variables even
  when given a smaller environment object. The focused test confirms arbitrary
  Host variables and non-allowlisted credential values remain absent. This is
  accepted as a documented platform boundary, not a review finding.

### Required follow-up

- Add the fatal UTF-8 rejection test and smallest runner fix.
- Run the complete focused command, type check, and two unchanged full-suite
  runs; update the handoff with exact Git object IDs and results.

### Roadmap and release impact

- M5 remains in progress until all milestone exit criteria pass.
