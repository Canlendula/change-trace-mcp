{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "change-trace": {
      "type": "local",
      "command": ["npx", "-y", "change-trace-mcp@<VERSION>"],
      "enabled": true,
      "timeout": 10000
    }
  }
}
