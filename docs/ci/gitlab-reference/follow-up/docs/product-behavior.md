# Service status behavior

`CTGR-001` defines two approved results. The service reports `operational`
with `Service is operational.` when planned maintenance is inactive. It reports
`maintenance` with `Scheduled maintenance is in progress.` when planned
maintenance is active.
